import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Future: Add images.remotePatterns for R2 URLs per client
  // Future: Add redirects/rewrites per client if needed
}

export default nextConfig
